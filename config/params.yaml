lidar_preprocess_node:
  ros__parameters:
    # Distance filter (minimum distance from origin)
    min_distance: 0.5

    # Height filter (z-axis)
    height_min: -2.0
    height_max: 5.0

    # Noise filter (Statistical Outlier Removal)
    sor_mean_k: 50
    sor_stddev: 1.0

    # Enable/disable filters
    enable_distance_filter: true
    enable_height_filter: true
    enable_noise_filter: true
    enable_footprint_filter: true
